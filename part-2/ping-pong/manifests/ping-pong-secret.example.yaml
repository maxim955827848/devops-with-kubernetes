# ex 2.05 — a demo Secret injected into ping-pong as SECRET_TOKEN.
# Copy to ping-pong-secret.yaml (gitignored), set a value, and apply with -n exercises.
apiVersion: v1
kind: Secret
metadata:
  name: ping-pong-secret
type: Opaque
stringData:
  SECRET_TOKEN: change-me-token
